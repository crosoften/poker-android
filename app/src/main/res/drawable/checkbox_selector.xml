<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado quando o CheckBox está marcado -->
    <item android:drawable="@drawable/checkbox_redonda_unchecked" />
    <!-- Estado padrão quando o CheckBox não está marcado -->
    <item android:drawable="@drawable/checkbox_redonda_checked" android:state_checked="true"/>
</selector>